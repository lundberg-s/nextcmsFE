import { EditTitle } from "./Edit";
import { RenderTitle } from "./Render";
import { PreviewTitle } from "./Preview";

export const Edit = { Title: EditTitle };
export const Render = { Title: RenderTitle };
export const Preview = { Title: PreviewTitle };