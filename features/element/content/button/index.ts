import {  EditButton } from './Edit';
import { RenderButton } from './Render';
import { PreviewButton } from './Preview';

export const Edit = { Button: EditButton };
export const Render = { Button: RenderButton };
export const Preview = { Button: PreviewButton };